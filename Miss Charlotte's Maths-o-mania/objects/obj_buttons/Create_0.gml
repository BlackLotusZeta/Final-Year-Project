// size of button and height
xMenu = x;
yMenu = y;

hButton = 64;

// Buttons array
button[0] = "START";
button[1] = "QUIT";

buttons = array_length(button);

// index of buttons
menuIndex = 0;
lastSelected = 0;